export default class Qtl {
  constructor(name, chromosome, position, min, max) {
    this.name = name;
    this.chromosome = chromosome;
    this.position = position;
    this.min = min;
    this.max = max;
  }
}
